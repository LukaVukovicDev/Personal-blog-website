/**
 * Produkciona konfiguracija (default). U developmentu se zamenjuje sa
 * environment.development.ts preko `fileReplacements` u angular.json.
 */
export const environment = {
  production: true,
  apiUrl: '/api',
  hubsUrl: '/hubs',
};
